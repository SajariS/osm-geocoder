[project]
name = "osm-proto"
version = "0.1.0"
description = ""
authors = [
    {name = "SajariS",email = "santeri.sajari@gmail.com"}
]
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "flask (>=3.1.2,<4.0.0)"
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"

[tool.poetry.scripts]
start = "src.server:main"

[tool.poetry]
packages = [
    { include = "src"}
]